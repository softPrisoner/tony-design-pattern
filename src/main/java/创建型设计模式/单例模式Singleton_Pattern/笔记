单例模式定义:
一个类只有一个实例，且该类能自行创建这个实例的一种模式。
单例模式有3个特点:
1、单例类只有一个实例对象
2、该单例对象必须由单例类自行创建
3、单例类对外提供一个访问该单例的全局访问点
为什么使用单例模式？
如果在系统中不使用单例模式的话，在碰到多线程访问的时候，printer就会给要请求的类，
分别在内存中new出一个printer对象，让这些请求的类去做print方法。这样大量占有内存，
就会导致系统运行变慢，像电脑的CPU一样，占有量极高，电脑卡死不动的感觉